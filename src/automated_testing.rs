/* --> Summary of contents

	[Automated Testing]
	
	Struct pointSingleType<T>
		Forces both x & y fields to be the same type T

		
 Summary of contents <-- */
 
 #[cfg(test)]
fn it_works() {
	let result = 2 + 2;
	assert_eq!(result, 4);
	}
}